{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="mt-3 alert alert-{{label}}" role="alert">
            {{ message |raw }}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    {% endfor %}
{% endfor %}